package mian.java;

class Solution {
    public String decodeString(String s) {
        String cnt = "";
        String k = "";
        char[] c = s.toCharArray();
        for (int i = 0; i < c.length; i++) {
            if (c[i] != '[') {
                cnt += c;
            if()
        }

    }
}